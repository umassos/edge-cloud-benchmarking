apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keras-model-serving.fullname" . }}-test-connection"
  labels:
    {{- include "keras-model-serving.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keras-model-serving.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
